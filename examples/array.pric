# -*- mode: ruby -*-

# ./pricrun examples/array.pric

def main()
  var a;
  var [2]b;
  var c;

  a         = 65; # A
  *(&b)     = 66; # B
  *(&b + 1) = 67; # C
  c         = 68; # D

  putchar(a);
  putchar(*(&b));
  putchar(*(&b + 1));
  putchar(c);
end
